export enum Response {
  BUILD_SAVED = 'build saved',
  UNKNOWN_BUILD = 'unknow build',
  DEFAULT = 'default message',
  COMMAND_RUN_ERROR = 'There was an error while executing this command!',
  GET_BUILDS = 'Result',
}
